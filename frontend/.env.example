# Optional: point API requests directly at the backend (bypasses Vite proxy).
# Use this if you get "Unexpected token '<'" or HTML instead of JSON.
# Restart the dev server after changing.
# VITE_API_URL=http://localhost:3080
