# Frontend dev server port (default 3000). Used by `npm run dev` and by PM2 when running frontend.
# VITE_DEV_PORT=3000

# Optional: point API requests at the backend (bypasses Vite proxy). Use when running FE and BE separately.
# Restart the dev server after changing. Example: http://localhost:3080
# VITE_API_URL=http://localhost:3080

# Theme: primary and secondary colors (hex, bright). Used for buttons, links, accents.
# VITE_PRIMARY_COLOR=#805ad5
# VITE_SECONDARY_COLOR=#e16d37

# Optional: logo image URL shown in the header.
# VITE_LOGO_URL=https://example.com/logo.png